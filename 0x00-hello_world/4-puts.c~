#include <stdio.h>
/**
*main - this is a script that uses puts not printf
*and it has the output with return 0
*/
int main(void)
{
puts("\"Programming is like building a multilingual puzzle"\n);
return (0);
}
